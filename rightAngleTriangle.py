print("let's calculate the area of right angle triangle")

base = float(input("enter the base length "))
height = float(input("enter the height"))

area = 0.5*base*height

print("the area is ", area,"unit sq.")
